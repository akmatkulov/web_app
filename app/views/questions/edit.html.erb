<h2>Edit Question</h2>

<%= form_with model: @question do |f| %>

  <div>
      <%= f.label :title %>
      <%= f.text_field :title %>
  </div>

  <div>
      <%= f.label :body %>
      <%= f.text_area :body %>
  </div>

  <%= f.submit "Edit Question"%>
<% end %>